# Python >= 3.10
# If you have an NVIDIA GPU, install the CUDA build of torch/torchvision FIRST
# (https://pytorch.org/get-started/locally/), then `pip install -r requirements.txt`.
torch>=2.4
torchvision>=0.19
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
PyYAML>=6.0
tqdm>=4.66
